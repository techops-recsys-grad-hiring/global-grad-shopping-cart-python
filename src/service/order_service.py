from src.model.order import Order
from src.model.customer import Customer

def show_confirmation(customer = Customer, products = [], totalPrice = float, loyaltyPointsEarned = int):
 # show confirmation
 # do some calculations and formatting on the shopping cart data and ask user for confirmation
 # after confirmation, redirect to place order
 return

def placeOrder(customer = Customer, products = []):
    # place order
    return Order(customer, products)